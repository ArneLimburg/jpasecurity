<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE article PUBLIC "-//OASIS//DTD Simplified DocBook XML V1.1//EN" "http://www.oasis-open.org/docbook/xml/simple/1.1/sdocbook.dtd">
<article id="setup">
  <title>Setup</title>
  <para>
    The application developed in this tutorial is based on the petclinic-sample of the Spring Framework.
    Please download <systemitem>spring-framework-2.5.6-with-docs.zip</systemitem>
    <ulink url="http://www.springsource.com/download/community?project=Spring%20Framework">from here</ulink>.
    This tutorial was written with the sample delivered with version 2.5.6 of the Spring Framework
    and may or may not work with other versions. In the zip-file within the base-folder <filename>spring-framework-2.5.6/</filename>
    you find a folder named <filename>samples</filename>. Within that folder there is a folder named <filename>petclinic</filename>.
    Please unpack this folder. It is the only folder we need from that zip.
  </para>
  <para>
    Like in the simple-tutorial we want to use maven 2 to build and deploy our application.
    So the first step is to put a <filename>pom.xml</filename> file into the folder <filename>petclinic</filename> with the following content.
  </para>
  <programlisting>
    <![CDATA[
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>
  <groupId>net.sf.jpasecurity</groupId>
  <artifactId>jpasecurity-spring-petclinic-sample</artifactId>
  <packaging>war</packaging>
  <version>0.3.1-SNAPSHOT</version>
  <name>JPA Security Spring Petclinic Sample</name>
  <url>http://jpasecurity.sf.net</url>

  <build>
    <plugins>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <source>1.5</source>
          <target>1.5</target>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-antrun-plugin</artifactId>
        <executions>
          <execution>
            <phase>process-classes</phase>
            <configuration>
              <tasks>
                <java classname="org.apache.openjpa.enhance.PCEnhancer"
                      classpathref="maven.runtime.classpath"
                      dir="target/classes" fork="true" />
              </tasks>
            </configuration>
            <goals>
              <goal>run</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.mortbay.jetty</groupId>
        <artifactId>maven-jetty-plugin</artifactId>
        <version>6.1.10</version>
        <configuration>
          <stopKey>petclinic</stopKey>
          <stopPort>1199</stopPort>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <dependencies>
    <dependency>
      <groupId>net.sf.jpasecurity</groupId>
      <artifactId>jpasecurity</artifactId>
      <version>0.3.1-SNAPSHOT</version>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring</artifactId>
      <version>2.5.6</version>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-webmvc</artifactId>
      <version>2.5.6</version>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-core</artifactId>
      <version>2.0.4</version>
    </dependency>
    <dependency>
      <groupId>commons-dbcp</groupId>
      <artifactId>commons-dbcp</artifactId>
      <version>1.2.2</version>
    </dependency>
    <dependency>
      <groupId>org.apache.openjpa</groupId>
      <artifactId>openjpa</artifactId>
      <version>1.2.1</version>
    </dependency>
    <dependency>
      <groupId>hsqldb</groupId>
      <artifactId>hsqldb</artifactId>
      <version>1.8.0.7</version>
    </dependency>
  </dependencies>
</project>
    ]]>
  </programlisting>
  <para>
    To be conform with maven, we have to change the directory-structure:
  </para>
  <itemizedlist>
    <listitem>
      <para>
        Move the content of the folder <filename>src/</filename> into the folder <filename>src/main/java/</filename>
        (which you have to create first)
      </para>
    </listitem>
    <listitem>
      <para>
        Now create <filename>src/main/resources/</filename> and move all non-java-files from <filename>src/main/java/</filename>
        into it (it mainly is the subfolder <filename>META-INF/</filename> and the file <filename>jdbc.properties</filename>,
        all other files may safely be deleted)
      </para>
    </listitem>
    <listitem>
      <para>
        Move the content of the folder <filename>war/</filename> into the folder <filename>src/main/webapp/</filename>
        (which you have to create first)
      </para>
    </listitem>
    <listitem>
      <para>
        Move the file <filename>db/hsqldb/petclinic.script</filename> directly into the main folder
        (which is named <filename>petclinic</filename>)
      </para>
    </listitem>
  </itemizedlist>
  <para>
    We don't need the rest of the content of the folder <filename>db/</filename>, so you may safely delete it.
    The same applies to the folder <filename>test/</filename> and the ant build-files (namely <filename>build.bat</filename>,
    <filename>build.properties</filename>, <filename>build.xml</filename> and <filename>warfile.bat</filename>).
  </para>
</article>
