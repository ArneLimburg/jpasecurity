<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE article PUBLIC "-//OASIS//DTD Simplified DocBook XML V1.1//EN" "http://www.oasis-open.org/docbook/xml/simple/1.1/sdocbook.dtd">
<article id="dependencies">
  <title>Dependencies</title>
  <para>
    Our application uses Hibernate as persistence provider and HSQLDB as (in-memory-)database.
    So we have to add the appropriate dependencies to our pom.xml file
    (between the <systemitem>&lt;dependencies&gt;&lt;/dependencies&gt;</systemitem> section). As we don't need it,
    we replace the dependency to JUnit, that maven created:
  </para>
  <programlisting>
    <![CDATA[
    <dependency>
      <groupId>net.sf.jpasecurity</groupId>
      <artifactId>jpasecurity</artifactId>
      <version>0.1.0</version>
    </dependency>
    <dependency>
      <groupId>org.hibernate</groupId>
      <artifactId>hibernate-entitymanager</artifactId>
      <version>3.3.2.ga</version>
    </dependency>
    <dependency>
      <groupId>hsqldb</groupId>
      <artifactId>hsqldb</artifactId>
      <version>1.8.0.7</version>
    </dependency>
    ]]>
  </programlisting>
  <para>
    Hibernate needs the Transaction-API from sun.
    Download it <ulink url="http://repository.jboss.com/maven2/javax/transaction/jta/1.0.1B/jta-1.0.1B.jar">here</ulink>
    and install it to your local Maven repository with the following command:
  </para>
  <programlisting>
mvn install:install-file -DgroupId=javax.transaction -DartifactId=jta -Dversion=1.0.1B -Dpackaging=jar -Dfile=/path/to/jta.jar
  </programlisting>
  <para>
    Note that you have to provide the absolute path to the <systemitem>jta.jar</systemitem>.
  </para>
  <para>
    As no remote repository contains JPA Security yet, we need to install it to our local maven-repository, too.
    If you don't already have, you may download the jpasecurity-0.2.0.jar <ulink url="https://sourceforge.net/project/showfiles.php?group_id=219346">here</ulink>.
    We install it with the following command:
  </para>
  <programlisting>
mvn install:install-file -DgroupId=net.sf.jpasecurity -DartifactId=jpasecurity -Dversion=0.2.0 -Dpackaging=jar -Dfile=/path/to/jpasecurity-0.2.0.jar
  </programlisting>
  <para>
    Note again that you have to provide the absolute path to your <systemitem>jpa-security-0.2.0.jar</systemitem>.
  </para>
</article>
